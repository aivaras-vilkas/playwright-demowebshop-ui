name: Playwright Tests

permissions:
  contents: write
  pages: write
  id-token: write

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  update-snapshots:
    if: false # Disable snapshot updates for now. Change to true to enable.
    runs-on: ubuntu-latest

    env:
      TESTING_PAGE: ${{ secrets.TESTING_PAGE }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      # Chromium skipped, so no browser install here

      - name: Configure Git for pushing
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}

      - name: Update snapshots
        run: npx playwright test --update-snapshots

      - name: Commit updated snapshots
        run: |
          git fetch origin main
          git rebase origin/main || git rebase --skip
          git add .
          git commit -m "Update Linux snapshots" || echo "No changes to commit"
          git push

  test:
    # needs: update-snapshots # Uncomment if snapshot updates enabled
    timeout-minutes: 60
    runs-on: ubuntu-22.04

    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: chromium
      TESTING_PAGE: ${{ secrets.TESTING_PAGE }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - name: Cache node modules
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Cache Playwright Browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      # Install only Firefox + WebKit (Chromium skipped)
      - name: Install Playwright Browsers
        run: npx playwright install firefox webkit

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Allure Results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: allure-results
          path: allure-results/
          retention-days: 30

  publish-allure-report:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download Allure Results
        uses: actions/download-artifact@v8
        with:
          name: allure-results
          path: allure-results

      - name: Install Allure CLI
        run: npm install -g allure-commandline --no-progress

      - name: Generate Allure Report
        run: allure generate allure-results --clean -o allure-report

      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: allure-report

  deploy:
    needs: publish-allure-report
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5